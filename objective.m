%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
function val = objective(x,POS,NEG)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
n = size(POS,2); [p,n] = peScore(x,POS,NEG);
val = -min(mean(p>2),mean(n<2)) - 0.15*mean(p) + 0.15*mean(n);

